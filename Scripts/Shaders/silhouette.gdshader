shader_type canvas_item;


void fragment() {	
    // Get UV coordinates for the image
	//vec2 mask_uv = UV;
     ////Sample the mask texture at the current UV position
    //vec4 mask_color = texture(camera_tex, mask_uv);
	
	//if (mask_color.a < 0.1) {
		//discard; // Discard the pixel if it's the color
	//} else {
		//vec4 base_color = texture(TEXTURE, UV); // Render normal pixels
		//COLOR = base_color + vec4(0.0, 0.0, 0.15, 0.0); // Slight tint
	//}
	
    COLOR = COLOR;
	A
}
